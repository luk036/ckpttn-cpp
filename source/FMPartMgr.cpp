#include <ckpttn/FMPartMgr.hpp>
#include <ckpttn/spdlog_example.hpp>

/* The FMPartMgr template methods are defined inline in the header,
   so this file only exists for template instantiation purposes. */
